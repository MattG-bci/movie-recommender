version: "3"
services:
  test-db:
    image: "postgres:16.3"
    command: "-c fsync=off"
    environment:
     - POSTGRES_USER=test
     - POSTGRES_PASSWORD=test
     - POSTGRES_DB=test
    ports:
     - "5432:5432"
