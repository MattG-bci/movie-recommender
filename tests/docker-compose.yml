version: "3"
services:
  test-db:
    image: "postgres:16.3"
    command: "-c fsync=off"
    environment:
     - POSTGRES_USER=postgres
     - POSTGRES_PASSWORD=postgres
    ports:
     - "54320:5432"
